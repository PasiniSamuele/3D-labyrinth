class Wall extends PbrLabyrinthElement{
    constructor(structure, slot, parent, program, imageUrl){
        super(structure, slot, parent, program, imageUrl);
    }

    init(){
        super.init();
    }
}
